id: net.classicube.ClassiCube
runtime: org.freedesktop.Platform
runtime-version: '23.08'
sdk: org.freedesktop.Sdk
command: ClassiCubeLauncher
finish-args:
  - --socket=x11
  - --device=dri
  - --share=network
  - --share=ipc
  - --socket=pulseaudio
modules:
  - name: ClassiCube
    buildsystem: simple
    build-commands:
      - gcc src/*.c -o src/ClassiCube -O1 -DCC_BUILD_FLATPAK -DCC_BUILD_ICON -rdynamic -lm -lpthread -lX11 -lXi -lGL -ldl
      - install -Dm755 src/ClassiCube -t ${FLATPAK_DEST}/bin
      - install -Dm755 ClassiCubeLauncher -t ${FLATPAK_DEST}/bin
      - install -Dm644 net.classicube.ClassiCube.svg ${FLATPAK_DEST}/share/icons/hicolor/scalable/apps/net.classicube.ClassiCube.svg
      - install -Dm644 net.classicube.ClassiCube.desktop ${FLATPAK_DEST}/share/applications/net.classicube.ClassiCube.desktop
      - install -Dm644 net.classicube.ClassiCube.metainfo.xml ${FLATPAK_DEST}/share/metainfo/net.classicube.ClassiCube.metainfo.xml
    sources:
      - type: git
        url: https://github.com/sungsphinx/ClassiCube.git
        branch: flatpak-wmclass
      - type: script
        dest-filename: ClassiCubeLauncher
        commands:
         - mkdir -p ${XDG_DATA_HOME}/ClassiCube
         - cd ${XDG_DATA_HOME}/ClassiCube && exec /app/bin/ClassiCube "$@"
      - type: file
        path: net.classicube.ClassiCube.svg
      - type: file
        path: net.classicube.ClassiCube.desktop
      - type: file
        path: net.classicube.ClassiCube.metainfo.xml
